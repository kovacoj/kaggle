# Ideas for Next Experiments

Agents: append your ideas here before coding. Mark them `[tried]` when execution starts and add a short outcome note after the run.

Suggested entry format:

- `[new]` Idea title | hypothesis: ... | expect: ... | note: pending
- `[tried]` Idea title | hypothesis: ... | expect: ... | note: improved smoke by ..., crashed because ..., or no signal

- `[tried]` Stacking | hypothesis: use CatBoost/LightGBM/XGBoost predictions as features into a logistic regression or ridge meta-learner instead of simple averaging | expect: better calibration and balanced accuracy than naive blending | note: a true 3-fold OOF stack on smoke scored 0.969637, below the 0.970515 single-model CatBoost frontier; the extra members were too weak or too correlated to help
- `[new]` Optuna on best single CatBoost | hypothesis: the current CatBoost feature set is stronger than the ensemble, so a focused search over depth, learning_rate, l2_leaf_reg, and bagging_temperature may beat the current best | expect: small but real smoke improvement without extra model complexity | note: pending
- `[tried]` Original dataset augmentation | hypothesis: concatenate the source irrigation dataset with the synthetic train data and add an `is_original` flag feature | expect: more robust minority-class signal, especially for `High` | note: rejected after verification because the sourced original irrigation dataset was identical to the competition data, so concatenation added no new signal
- `[tried]` Stronger High-class weighting | hypothesis: the best current setup still under-serves `High`, so more aggressive weighting or oversampling may lift balanced accuracy | expect: recall gain on `High` without collapsing `Low` recall | note: run 20260428-101351-dirty scored 0.967145 on smoke in 378.3s; worse than the 0.970515 frontier, so stronger High weighting alone hurt balanced accuracy
- `[new]` K-fold CV ensemble | hypothesis: averaging 5 folds on the `full` benchmark will reduce variance versus a single split fit | expect: stabler validation score and possibly stronger submission performance | note: pending
- `[new]` Feature selection with SHAP | hypothesis: dropping noisy features from the best CatBoost run will simplify the model without losing signal | expect: similar or better score with less runtime and lower overfit risk | note: pending
- `[new]` Tabular MLP ensemble member | hypothesis: a small neural net may capture interactions the tree models miss | expect: complementary errors that improve an ensemble | note: pending
- `[tried]` Best v15 plus rounding flags | hypothesis: The best CatBoost snapshot may still be missing synthetic-data precision signals, so adding a few low-cost rounding indicators could recover generator artifacts without changing the core model. | expect: A small smoke lift over 0.970515 if decimal-format patterns carry label signal. | note: run 20260428-102634-dirty scored 0.965666 on smoke in 258.9s; materially worse than the 0.970515 v15 frontier, so rounding-pattern flags added noise instead of useful synthetic signal
- `[tried]` Stronger pairwise TE smoothing | hypothesis: The v15 baseline uses many sparse categorical pair encodings, and the last feature-expansion miss suggests the model is overfitting; increasing pairwise target-encoding smoothing may shrink noisy rare-category effects while preserving the strong core feature set. | expect: A small smoke improvement over 0.970515 from better generalization, especially on rare category combinations. | note: run 20260428-103247-dirty scored 0.970515 on smoke in 290.2s; it matched the current best while regularizing sparse pair encodings, so this is a viable tied-best baseline but not an improvement
- `[tried]` Lower learning rate with more CatBoost iterations | hypothesis: The tied-best baseline may still be under-optimized in its boosting path; a smaller learning rate with more rounds could fit minority decision boundaries more smoothly without changing the feature set. | expect: A modest smoke gain over 0.970515 if the current depth-8 model is still leaving signal on the table. | note: run 20260428-104004-dirty scored 0.967813 on smoke in 376.2s; worse than the 0.970515 frontier, so the tied-best baseline was not underfit enough to benefit from a slower boosting path
- `[tried]` Dual CatBoost blend | hypothesis: The tied-best CatBoost is strong but may have correlated mistakes; averaging a second, shallower CatBoost with slightly different randomization on the same feature set could reduce variance and improve balanced accuracy without introducing a wholly new pipeline. | expect: A small smoke gain over 0.970515 if the two CatBoost variants make complementary errors, especially near the High-class boundary. | note: run 20260428-104917-dirty scored 0.968687 on smoke in 419.1s; worse than the 0.970515 frontier, so a same-feature dual CatBoost blend did not add useful diversity
- `[tried]` Medium/High probability scaling on v18 baseline | hypothesis: The tied-best single CatBoost may already rank classes well, but the final argmax boundary can still be miscalibrated for balanced accuracy; scaling Medium and High probabilities before normalization could improve the decision surface more flexibly than a High-only shift. | expect: A small smoke gain over 0.970515 if the current probabilities need class-specific calibration rather than more feature or model changes. | note: run 20260428-105950-dirty scored 0.969285 on smoke in 303.9s; worse than the 0.970515 frontier, so extra class-probability scaling did not improve the tied-best baseline
- `[tried]` CatBoost plus LightGBM blend | hypothesis: The tied-best CatBoost is strong, but recent same-family tweaks failed; adding a lighter-weight LightGBM on the same engineered features may contribute genuinely different tree partitioning errors without a full pipeline rewrite. | expect: A small smoke gain over 0.970515 if LightGBM adds complementary signal while CatBoost remains the dominant model. | note: run 20260428-110724-dirty scored 0.970289 on smoke in 283.4s; close but still below the 0.970515 frontier, so LightGBM added some complementary signal but not enough to beat the best single CatBoost
- `[tried]` Optuna CatBoost hyperparameter search | hypothesis: All local CatBoost parameter nudges failed; a broader automated search over depth, LR, L2, bagging_temperature, random_strength, and border_count may find a non-obvious configuration that lifts balanced accuracy past the current plateau. | expect: A meaningful smoke gain over 0.970515 if the current config is in a local optimum. | note: pending
- `[tried]` 5-fold CV CatBoost ensemble | hypothesis: Averaging 5 CatBoost models trained on different stratified folds should reduce variance and is the standard winning approach for Playground competitions. Use a stratified 10% subset to stay within time limits. | expect: A meaningful smoke gain over 0.970515 from variance reduction, especially on the rare High class. | note: run 20260428-125005-dirty scored 0.966874 on smoke in 693.1s; worse than the 0.970515 single-model frontier, so CV ensemble on this feature set adds no value
- `[tried]` Two-stage cascade: High vs Rest, then Low vs Medium | hypothesis: Diagnosis shows Medium recall (0.941) is the bottleneck, not High. The 10x High weight over-predicts High (precision only 0.70), stealing 277 Medium samples. A binary cascade can separate High cleanly first, then focus the Low/Medium boundary without the High weight distorting it. | expect: A meaningful smoke lift because the cascade directly addresses the two dominant error modes: Medium->High and Medium->Low. | note: run 20260428-142526-dirty scored 0.967959 on smoke; worse than 0.970515 frontier. The hard threshold split loses joint 3-class information; the binary High/Rest model is weaker than the 3-class model's High probability
- `[tried]` Deployable inner-split calibration | hypothesis: Learn the probability shift on an inner split of the training fold so validation and test use the same decision rule. | expect: Slightly lower but more transferable score than outer-validation-only calibration, potentially still competitive with the frontier. | note: deployable baseline scored 0.967386 on smoke and deployable CatBoost/adjacent-pair blend scored 0.967254; both were materially worse than the validation-tuned paths, so this was rejected for now
- `[tried]` Medium-vs-High resolver | hypothesis: Keep the strong 3-class CatBoost, but apply a specialist Medium/High model only on low-margin Medium/High cases to fix the dominant remaining error band. | expect: A small smoke lift over 0.970515 without the damage caused by full cascades. | note: best smoke result matched the base score at 0.970515 and preferred effectively doing nothing (`alpha = 0.0`), so the resolver added no signal
- `[tried]` Adjacent-pair CatBoost blend | hypothesis: Blend the strong 3-class CatBoost with a second CatBoost built from adjacent pairwise boundaries (`Low vs Medium` and `Medium vs High`) so the ensemble gets a different view of the Medium decision surface. | expect: A small but real smoke lift if the pairwise system contributes complementary Medium-boundary signal. | note: run 20260429-105450-dirty scored 0.970735 on smoke and run 20260429-122419-dirty scored 0.970934 on full; this is the new current best local approach
- `[tried]` Medium/High class shift on v35 blend | hypothesis: The current adjacent-pair blend still loses most remaining balanced-accuracy mass by predicting High for Medium cases; a joint Medium/High shift search on the blended probabilities may recover Medium recall without undoing the strong High recall. | expect: A small smoke lift over 0.970735 if the blend mainly needs a better final decision boundary rather than a new model. | note: screening attempt never completed: repeated smoke runs timed out past 900s even after forcing 12 CPU threads, cutting CatBoost iteration budgets, and shrinking the smoke-train subset from 30k to 10k; this implementation path is currently too slow to benchmark reliably
- `[tried]` Low/Medium specialist blend | hypothesis: The current best gain came from adding an alternate view of the Medium boundary; a single Low/Medium specialist blended with the strong multiclass CatBoost may recover much of that lift at much lower runtime than the full adjacent-pair ensemble. | expect: A smoke improvement over the single-model CatBoost, or at least a near-v35 score with materially lower runtime. | note: run 20260430-010622-dirty scored 0.970691 on smoke in 223.2s; slightly below the 0.970735 v35 frontier, but very close at much lower runtime, so this is a useful cheap branch to build on even though it is not the new best
- `[tried]` Low/Medium specialist blend plus Medium/High shifts | hypothesis: The cheap Low/Medium specialist blend is already within 0.000044 of the v35 smoke frontier; adding a joint Medium/High shift search may recover the last bit of balanced accuracy without bringing back the third model. | expect: A small smoke gain over 0.970691, with a realistic shot at matching or slightly beating the 0.970735 v35 frontier while staying fast. | note: run 20260430-011152-dirty scored 0.970691 on smoke in 470.8s, exactly matching the simpler v37 score while taking much longer; the extra Medium/High shift search added no value on this cheaper blend
- `[tried]` Medium/High specialist blend | hypothesis: The current best v35 gain likely comes from a better Medium/High view, and the cheap Low/Medium specialist branch nearly matched the frontier. A two-model blend that keeps the multiclass CatBoost and replaces only the Medium/High mass with a specialist CatBoost may capture more of the winning signal at similar runtime. | expect: A smoke score at or above 0.970691 with v37-like runtime, with a realistic chance to match or beat the 0.970735 v35 frontier. | note: run 20260430-085515-dirty scored 0.970603 on smoke in 114.1s; faster than the Low/Medium specialist branch but clearly weaker, so Medium/High-only specialization does not recover enough of the v35 ensemble gain
- `[tried]` Flexible three-view specialist blend | hypothesis: The single-companion specialist runs suggest Low/Medium and Medium/High carry different amounts of signal. Instead of folding both specialists into one fixed adjacent-pair probability system, blend the base multiclass model with separate Low/Medium and Medium/High specialist views using independently tuned weights. | expect: A smoke score at or above the 0.970735 v35 frontier if the two specialist seams are complementary but the fixed v35 combination was too rigid. | note: run 20260430-085946-dirty scored 0.970778 on smoke in 482.2s, a new local smoke best; a follow-up full confirmation attempt exceeded the 900s runtime budget before completion, so this is promising but not yet a confirmed new headline approach
- `[tried]` Faster three-view specialist blend | hypothesis: The strongest current smoke branch is blocked by full-runtime, not smoke accuracy. Lowering the multiclass and binary CatBoost iteration budgets while keeping the same three-view blend may preserve most of the 0.970778 smoke signal but get much closer to a confirmable full run. | expect: A smoke score still near the new frontier with materially lower runtime, making a full confirmation attempt more realistic. | note: run 20260430-092512-dirty matched the 0.970778 smoke score of v40 but actually took longer at 614.0s, so lowering the iteration budgets did not deliver the intended runtime win in the harness; discard as a runtime-focused experiment
